{
  "name": "portal-backend",
  "version": "1.0.0",
  "description": "Product registration portal backend API",
  "main": "m10.ts",
  "type": "module",
  "scripts": {
    "start": "bun run m10.ts",
    "dev": "bun --hot run m10.ts",
    "build": "bun build m10.ts --target=bun --outfile=dist/index.js"
  },
  "dependencies": {
    "elysia": "^0.7.x",
    "@elysiajs/cors": "^0.7.x",
    "knex": "^2.5.x",
    "sqlite3": "^5.1.x"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
